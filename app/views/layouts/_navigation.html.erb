
 
        <% if current_user %>
        <ul class="nav navbar-nav">
           
         <li class="dropdown">
           <a href="#" class="dropdown-toggle" data-toggle="dropdown">System <span class="caret"></span></a>          
           <ul class="dropdown-menu" role="menu">
                <li> <%= link_to "Users", users_path %> </li>                
              </ul>                
            </li>
        </ul>
        <% end %>
       
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">User <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <% if current_user %>
               <li><%= link_to('<span class="glyphicon glyphicon-cog"></span> Settings'.html_safe, edit_user_registration_path) %></li>
               <li><%= link_to("<span class='glyphicon glyphicon-off'></span> #{t('.logout')}".html_safe, destroy_user_session_path, method: 'delete') %></li>
               <% else
                  %>
               <li><%= link_to("<span class='glyphicon glyphicon-log-in'></span> #{t('.login')}".html_safe, new_user_session_path) %></li>
               <li><%= link_to('<span class="glyphicon glyphicon-saved"></span> Register'.html_safe, new_user_registration_path) %></li>
               <% end %>
               <li><%= link_to "Português", locale: "pt-BR" %></li>
              <li><%= link_to "English", locale: "en" %></li>
                
              </ul>
            </li>
        </ul>
   